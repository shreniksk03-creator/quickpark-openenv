[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "quickpark-openenv"
version = "0.1.0"
description = "QuickPark Admin Simulation"
authors = [
  { name="Shrenik" }
]
dependencies = [
    "pydantic",
    "openai",
    "fastapi",
    "uvicorn"
]
